Options -MultiViews
RewriteEngine On
RewriteBase /

# Force HTTPS first so search engines only see the secure canonical version
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://onea.africa%{REQUEST_URI} [R=301,L]

# Canonical host: keep all public URLs on non-www onea.africa
RewriteCond %{HTTP_HOST} ^www\.onea\.africa$ [NC]
RewriteRule ^ https://onea.africa%{REQUEST_URI} [R=301,L]

# Remove trailing slashes from page URLs while preserving real directories.
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{THE_REQUEST} \s/+(.+?)/+[?\s] [NC]
RewriteRule ^(.+?)/+$ /$1 [R=301,L,NE]

<IfModule mod_headers.c>
  Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=(), usb=(), browsing-topics=()"
  Header always set Content-Security-Policy "default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'self'; form-action 'self'; script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://www.google.com https://www.gstatic.com https://www.clarity.ms https://scripts.clarity.ms https://connect.facebook.net; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: blob: https:; connect-src 'self' https://www.google-analytics.com https://analytics.google.com https://www.googletagmanager.com https://www.clarity.ms https://*.clarity.ms https://connect.facebook.net https://www.facebook.com; frame-src 'self' https://www.googletagmanager.com https://www.google.com https://recaptcha.google.com;"

  <FilesMatch "\.(?:css|js|mjs|woff2?|webp|png|jpe?g|gif|svg|ico)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>

  <FilesMatch "\.(?:html?)$">
    Header set Cache-Control "no-cache"
  </FilesMatch>
</IfModule>

<FilesMatch "(^\.env|\.log$|\.jsonl$|\.lock$|service-account.*\.json$)">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
  </IfModule>
</FilesMatch>

<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
</IfModule>

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/css
  AddOutputFilterByType DEFLATE application/javascript application/json application/xml
  AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>

# Legacy public URLs from the previous site
RewriteRule ^about/?$ /team [R=301,L]
RewriteRule ^contact/?$ / [R=301,L]
RewriteRule ^client-login/?$ /client-portal [R=301,L]
RewriteRule ^improve-how-you-present-your-brandblog/?$ /blog [R=301,L]

# Serve existing files directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Never rewrite missing build assets to the SPA document.
# A missing JavaScript file served as text/html leaves the app blank.
RewriteRule \.(?:css|js|mjs|map|json|png|jpe?g|gif|webp|svg|ico|woff2?|ttf|tsx?)$ - [R=404,L,NC]

# Pre-rendered public routes
RewriteRule ^$ index.html [QSA,L]
RewriteRule ^connectivity/?$ connectivity/index.html [QSA,L]
RewriteRule ^solutions/?$ solutions/index.html [QSA,L]
RewriteRule ^pricing/?$ pricing/index.html [QSA,L]
RewriteRule ^apple-device-procurement/?$ apple-device-procurement/index.html [QSA,L]
RewriteRule ^telkom-application/?$ telkom-application/index.html [QSA,L]
RewriteRule ^client-portal/?$ client-portal/index.html [QSA,L]
RewriteRule ^team/?$ team/index.html [QSA,L]
RewriteRule ^careers/?$ careers/index.html [QSA,L]
RewriteRule ^blog/?$ blog/index.html [QSA,L]
RewriteRule ^case-studies/?$ case-studies/index.html [QSA,L]
RewriteRule ^privacy/?$ privacy/index.html [QSA,L]
RewriteRule ^terms/?$ terms/index.html [QSA,L]
RewriteRule ^solutions/(openserve-business-fibre|lte-enterprise-packages|home-wifi-networking|managed-it-support|corporate-digital-marketing)/?$ solutions/$1/index.html [QSA,L]

# Pre-rendered dynamic public routes. New CMS content falls back to the SPA
# until its static page is included in a later build.
RewriteCond %{REQUEST_FILENAME}/index.html -f
RewriteRule ^blog/([^/]+)/?$ blog/$1/index.html [QSA,L]
RewriteRule ^blog/[^/]+/?$ index.html [QSA,L]

RewriteCond %{REQUEST_FILENAME}/index.html -f
RewriteRule ^case-studies/([^/]+)/?$ case-studies/$1/index.html [QSA,L]
RewriteRule ^case-studies/[^/]+/?$ index.html [QSA,L]

# Authenticated and operational SPA routes
RewriteRule ^telkom-sign/[^/]+/?$ index.html [QSA,L]
RewriteRule ^launch-platform(?:/.*)?$ index.html [QSA,L]

# Unknown routes must return a real HTTP 404 rather than a soft-404 SPA page.
RewriteRule ^ - [R=404,L]
